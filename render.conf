# Render deployment configuration
port = 10000
threads = 8
queue_capacity = 512
keepalive_timeout = 5
keepalive_requests = 100
document_root = /srv/http/public
log_file = -
database_path = /srv/http/data/dense-http.db
template_root = /srv/http/templates
